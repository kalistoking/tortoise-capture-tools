# --- captured / extracted material: never versioned ---------------------
*.pcap
*.pcapng
*.jsonl
*.session-key
session_key*.txt
data/
out/
export/
logs/

# --- local configuration (machine-specific paths) ------------------------
tct.toml

# --- cross-project working state, owned elsewhere ------------------------
# TASKS.md is assembled and re-read by the board session from outside this
# repository. It is working state about the work, not part of the work, and
# a second copy of it in history is a second place for it to go stale.
TASKS.md

# --- generated artifacts ------------------------------------------------
*.sql.out
.cache/
opcodes.cache.json
updatefields.cache.json

# --- python -------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- editors / os -------------------------------------------------------
.vscode/
.idea/
Thumbs.db
desktop.ini

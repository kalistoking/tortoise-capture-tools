# --- captured / extracted material: never versioned ---------------------
*.pcap
*.pcapng
*.jsonl
*.session-key
session_key*.txt
data/
out/
export/
logs/

# --- local configuration (machine-specific paths) ------------------------
tct.toml

# --- internal development notes -----------------------------------------
# Tracking this work does not live in this repository at all: the task file is
# `tasks/tct.md` in the development manager, and that is the copy to edit. This
# line remains only so a re-created TASKS.md here cannot be committed by
# accident -- two copies of a plan diverge faster than anyone notices.
TASKS.md

# --- generated artifacts ------------------------------------------------
*.sql.out
.cache/
opcodes.cache.json
updatefields.cache.json

# --- python -------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- editors / os -------------------------------------------------------
.vscode/
.idea/
Thumbs.db
desktop.ini
